name: Test

on: [pull_request]
jobs:
  Test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:10.8
        env:
          POSTGRES_USER: runner
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: 'poporopo-test'
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
        - 5432:5432
        # needed because the postgres container does not provide a healthcheck
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '14'

      # - name: Start PostgreSQL on Ubuntu
      #   env:
      #     POSTGRES_HOST_AUTH_METHOD: "trust"
      #   run: |
      #     sudo systemctl start postgresql.service
      #     pg_isready
      # # Change user name and password to meet your requirements
      # - name: Create additional user
      #   run: |
      #     sudo -u postgres psql --command="CREATE USER runner" --command="\du"
      # # Change database name to meet your requirements
      # - name: Create additional database
      #   run: |
      #     sudo -u postgres createdb --owner=runner poporopo-test

      - run: npm ci
      - run: npm run test
